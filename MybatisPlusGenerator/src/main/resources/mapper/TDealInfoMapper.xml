<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kirck.mapper.TDealInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kirck.entity.TDealInfo">
        <id column="id" property="id" />
        <result column="deal_id" property="dealId" />
        <result column="deal_info" property="dealInfo" />
        <result column="user_notes" property="userNotes" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, deal_id, deal_info, user_notes
    </sql>

</mapper>
