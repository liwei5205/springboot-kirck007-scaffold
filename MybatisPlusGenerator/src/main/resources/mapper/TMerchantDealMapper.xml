<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kirck.mapper.TMerchantDealMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kirck.entity.TMerchantDeal">
        <id column="id" property="id" />
        <result column="merchant_id" property="merchantId" />
        <result column="deal_title" property="dealTitle" />
        <result column="price" property="price" />
        <result column="store_price" property="storePrice" />
        <result column="appUrl" property="appUrl" />
        <result column="notes" property="notes" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, merchant_id, deal_title, price, store_price, appUrl, notes
    </sql>

</mapper>
